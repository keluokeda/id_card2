## 使用方法
#### 1.添加abiFilters；
```
ndk {
            // 设置支持的SO库架构
            abiFilters 'armeabi' //, 'x86', 'armeabi-v7a', 'x86_64', 'arm64-v8a'
        }
```
2.替换开发码
```
Devcode.devcode="QMVPAMLUZYBXAW5";
```
